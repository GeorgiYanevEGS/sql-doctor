__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
*.egg-info/
.pytest_cache/

# Never commit credentials
.env.local
config.local.yaml

# PyInstaller build artifacts
dist/
build/
*.spec.bak
# Auto-generated by `flet pack` on every GUI build (unlike the hand-maintained
# sql-doctor.spec used by the CLI build, which IS tracked).
sql-doctor-gui.spec

# coverage_ledger.json is a build-time copy placed here by build.py;
# the authoritative copy lives in tests/coverage_ledger.json.
core/data/coverage_ledger.json
